# App submission template

Copy this directory to a new top-level `app-id/` directory. Replace every
placeholder, add a rights-cleared `icon.png`, and follow
[CONTRIBUTING.md](../../CONTRIBUTING.md). Keep the review status `proposed` and
do not mark maintainer lifecycle tests as complete.
